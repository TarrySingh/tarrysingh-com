# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Synaptic Cartography handover bundle staging
# (contents extracted to docs/synaptic/, design/, proposals/, .claude/commands/)
/tch-v2/
/synaptic-cartography-handover-bundle-v2.zip
.env*.local

# Humanoid Ascendancy handover bundle — design reference only.
# Ported into src/app/humanoid + src/components/humanoid (+ public/humanoid).
# The source bundle (HTML/JSX decks, 120MB full-deck PNGs) stays out of git.
/docs/humanoid-robotics/

# Live sweep output (regenerated by scripts/live-slop-sweep.mjs)
live-sweep-report.json
